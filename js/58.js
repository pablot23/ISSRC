/*function esPar(numero) {
 
 return (numero % 2) == 0;

}

var numero = 7; // Aquí puedes cambiar el número que deseas verificar

console.log(esPar(numero));*/



function esPar(numero) {
  
    return (numero % 2) == 0;
    
    }
    
    let numero = 8; // Puedes cambiar el número aquí
    
    let resultado = esPar(numero);
    
    console.log(resultado);

    /*function esPar (numero) {
        let resto = numero % 2;

        return resto == 0;
    }
    let entrada= prompt("ingresar el numero")
    //console.log(esPar(entrada));// 
    console.log(`El numero ingrsado ${esPar(entrada) ? 'es par' : 'es impar'})
    */

    /*yo la hice así
function esPar(nume){
    if(nume%2 === 0  ){
        console.log("es par");
    } else{
        console.log("es impar");
    }
} */