    function invierteCadena(cadena) {
    return cadena.split('').reverse().join('');
  } 

  /*En esta función, utilizamos los siguientes métodos de JavaScript para invertir la cadena:

    split(''): Este método divide la cadena en un arreglo de caracteres individuales.
    reverse(): Este método invierte el orden de los elementos en el arreglo.
    join(''): Este método une los elementos del arreglo en una cadena nuevamente. */


  /*function invierteCadena(cadena) {
    let cadenaInvertida= "";

    for (i = 0; i<=10 ;i++) {

    }

    return cadenaInvertida;
  }
  let cadenaAInvertir= "hola"

  console.log(cadenaAInvertir)*/